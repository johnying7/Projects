using UnityEngine;
using System.Collections;

public class WellInfo : MonoBehaviour {
}

/*
 * This is the default script that details what scripts, objects, etc. need
 * to be applied to each prefab.
 * 
 * 
 * - The well must have a well cover object (well cover must have a mask material attached to render before terrain)
 * - Well mesh must have a mesh collider and the SetRenderQueue script added
*/